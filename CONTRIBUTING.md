# 贡献指南

使用 `uv sync --locked` 安装环境。提交前运行 README 中的检查命令，行为修改补充相应测试，接口和使用方式修改同步更新文档。

运行代码放在 `src/agent_harness_lab`，测试通过安装后的包导入。公开接口提供类型标注和简明文档；对外部输入进行校验。按课程实际需要引入依赖和模块，不预建空层次或兼容机制。

真实模型请求必须显式启用，常规测试不消耗 API 配额。不提交 `.env`、凭据、真实用户数据或本地执行产物。

课程发布应标记固定代码版本，记录启动命令、预期结果和已知限制。尚未实现的功能必须明确标注为规划。
